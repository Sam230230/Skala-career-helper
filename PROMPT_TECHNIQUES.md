# 적용 프롬프트 기법 정리 (기획서 수록용)

본 서비스의 에이전트 프롬프트는 아래 9가지 기법을 조합해 설계했다.
각 기법은 "왜 필요한가(문제) → 어디에 적용했는가(위치) → 어떻게 적용했는가(예시)" 순으로 정리한다.

| # | 기법 | 해결하는 문제 | 적용 위치 |
|---|---|---|---|
| 1 | Role Prompting | 일반론적 답변 | agents.yaml `role` / `backstory` |
| 2 | Constraint Prompting | 환각·창작 | 전 task `준수 규칙` |
| 3 | Chain-of-Thought (단계 지시) | 얕은 분석 | 전 task `절차` 블록 |
| 4 | Delimiter / XML Tagging | 입력 3종 혼선 | task 1 입력부 |
| 5 | Schema Enforcement | 파싱 불가 출력 | `output_pydantic` |
| 6 | Rubric Prompting | 점수 일관성 부족 | task 3 적합도 루브릭 |
| 7 | Negative Prompting | 상투적 표현 | task 1·3 금지 문구 |
| 8 | Grounding / Citation | 근거 없는 추천 | 근거 인용·출처 기재 |
| 9 | Task Decomposition | 단일 프롬프트 과부하 | 에이전트 4분할 |

---

## 1. Role Prompting (역할 부여)
**문제** — "자기소개서를 분석해줘"만 주면 모델은 누구나 할 수 있는 일반론을 낸다.
**적용** — 각 에이전트에 경력 연차·처리 건수·직업적 습관까지 부여했다.
예: `12년간 3만 건 이상의 서류를 읽어온 서류 분석 전문가`, `연간 500명 이상을 면접한 현직 면접관`.
backstory에는 "미사여구는 역량으로 인정하지 않는다" 같은 **행동 원칙**을 함께 심어
페르소나가 곧 제약으로 작동하게 했다.

## 2. Constraint Prompting (제약 명시)
**문제** — LLM이 없는 경험·수치·기업명을 그럴듯하게 만들어낸다. 컨설팅에서는 치명적이다.
**적용** — 모든 task 끝에 `[준수 규칙]` 블록을 고정 배치.
- 원문에 없으면 "정보 없음"
- 검색 미확인 수치는 "데이터 확인 불가", 추정 시 "(추정)" 병기
- 개인식별정보 출력 금지
- 나이·성별·학교 서열 등 차별 요소 평가 반영 금지

## 3. Chain-of-Thought (사고 절차 지시)
**문제** — 요약과 추천을 한 번에 시키면 근거 없이 결론부터 낸다.
**적용** — task마다 `1단계 정규화 → 2단계 키워드 추출 → 3단계 증거 매핑 → …` 로
사고 순서를 명시하고, **"내부적으로 사고한 뒤 최종 결과물만 출력하라"** 로 중간 과정 노출은 차단했다.
(추론 과정은 품질에 쓰고, 사용자에게는 결과만 보여주는 구조)

## 4. Delimiter / XML Tagging (입력 구분)
**문제** — 자기소개서·이력서·포트폴리오 3종이 하나의 string으로 들어오면 경계가 뭉개진다.
**적용** — `<자기소개서> … </자기소개서>` 형태로 태그 분리.
추가로 "문서 간 내용이 충돌하면 이력서를 우선한다"는 **충돌 해소 규칙**을 명시했다.

## 5. Schema Enforcement (구조화 출력 강제)
**문제** — 자연어 출력은 다음 에이전트가 파싱하지 못하고, 프론트 렌더링도 불가하다.
**적용** — `expected_output`에는 사람이 읽을 설명을, 실제 강제는 `output_pydantic`으로.
`Literal["상","중","하"]`, `Field(ge=0, le=40)`, `min_length=5, max_length=5` 등으로
값 범위와 개수까지 타입으로 고정했다.
> ⚠️ 구현 주의: CrewAI는 description을 문자열 보간하므로 YAML 본문에 JSON 중괄호를 쓰면
> KeyError가 난다. 스키마는 반드시 코드 쪽 Pydantic으로 강제할 것.

## 6. Rubric Prompting (평가 기준 고정)
**문제** — 매니저별·실행별 점수 편차 → 기획 목표인 "표준화"가 깨진다.
**적용** — 적합도 점수를 가중치로 분해했다.
기술스택 40 / 직무경험 30 / 도메인 15 / 소프트스킬 15,
해석 등급도 80↑ 즉시 지원 / 60~79 보완 후 지원 / 60↓ 직무 재설정으로 고정.
점수와 함께 **항목별 배점 내역**을 출력하게 해 검증 가능성을 확보했다.

## 7. Negative Prompting (금지 지시)
**문제** — "열정적", "성장하겠습니다", "AI 프로젝트 하나 더 하세요" 같은 무가치 출력.
**적용** —
- 태도 표현은 역량 근거로 인정 금지
- 개조식 요약 금지(서술형 강제)
- 막연한 학습 권유 금지 → 주제·스택·산출물·기간·난이도 필수 기재
- 예/아니오로 끝나는 폐쇄형 면접 질문 금지

## 8. Grounding / Citation (근거 결합)
**문제** — 추천의 설득력이 없으면 컨설턴트가 상담에 쓰지 못한다.
**적용** —
- 역량 키워드마다 원문 인용 30자 이내 첨부
- 채용 데이터마다 출처 플랫폼명 기재, 최근 12개월 자료로 한정
- 적합도 배지(✅/⚠️/❌)마다 판정 근거 인용 필수

## 9. Task Decomposition (역할 분할)
**문제** — 요약+시장조사+적합성평가+리포팅을 한 프롬프트에 넣으면 뒤쪽 지시가 무시된다.
**적용** — 4개 에이전트로 분할하고 `context`로 결과를 넘기는 sequential 파이프라인 구성.
특히 **리포트 에디터를 분리**해 "분석"과 "표현"을 나눴다.
에디터에게는 "새 사실 추가 금지, 재배열과 표현 개선만" 이라는 편집 원칙을 부여해
가독성을 높이면서도 내용 왜곡을 막았다.

---

## 확장 요구사항 대응
- **배치 처리**: `crew().kickoff_for_each(inputs=[...])` — 프롬프트는 1인 기준을 유지하고
  반복은 실행 레벨에서 처리해야 인원이 늘어도 품질이 떨어지지 않는다.
- **희망 진로 선택 입력**: 빈 값은 `"없음"`으로 정규화 후,
  (1) 미입력 → 서류 기반 추론 + `is_inferred=True`
  (2) 입력 → 그 직무를 1순위 후보에 포함하되 **정합성(상/중/하) 판정 + 대안 직무 제시**
  두 분기를 프롬프트에 명시했다.
